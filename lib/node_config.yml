{
  "capabilities":
		NODE_CAPABILITIES,
  "configuration":
  {
    "proxy": REMOTE_PROXY,
    "maxSession": 5,
    "port": HOST_PORT,
    "host": HOST_IP,
    "register": true,
    "registerCycle": 5000,
    "hubPort": HUB_PORT,
    "hubHost": HUB_IP
  }
}